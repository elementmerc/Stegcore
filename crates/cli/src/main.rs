// Stegcore CLI entry point — Session 6 will implement all subcommands.
// For now this stub exits with a placeholder message.

fn main() {
    eprintln!("Stegcore CLI — coming in Session 6");
    std::process::exit(1);
}
